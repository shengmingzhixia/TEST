<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">
    <!--使用默认的servlet去处理静态资源-->
    <!--<mvc:default-servlet-handler/>-->

    <!--处理器映射器：根据url找到handler-->
    <!--注解式的处理器映射器-->
<!--
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping"></bean>
-->
    <!--处理器适配器：适配不同类型的handler（原生servlet开发 注解开发）：注解开发-->
<!--
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter"></bean>
-->
    <import resource="classpath:applicationContext.xml"/>
    <!--开启注解扫描-->
    <context:component-scan base-package="cgy"/>

    <!--相当于上面两行：注解式的处理器映射器和适配器-->
    <mvc:annotation-driven/>

    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <!--视图前缀：视图所在的目录-->
        <property name="prefix" value="/WEB-INF/"/>
        <!--视图后缀-->
        <property name="suffix" value=".jsp"/>
    </bean>
    <!--文件上传配置：id为multipart-->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="50000000"/>
    </bean>
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/**"/>
            <!--首页放行 和 搜索放行-->
            <mvc:exclude-mapping path="/toLogin"/>
            <mvc:exclude-mapping path="/entertouristLogin"/>
            <mvc:exclude-mapping path="/toRegister"/>
            <mvc:exclude-mapping path="/logout"/>
            <bean class="cgy.interceptor.CheckLoginInc"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>